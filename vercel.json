{
  "framework": "nextjs",
  "buildCommand": "rm -rf .next && npx next build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/inngest",
      "schedule": "*/5 * * * *"
    }
  ]
}
